package nz.ac.massey.a1;
//use enum to restrict tokentypes
public enum TokenType {
    APPEND, LIST, EXIT, PRINT, PRINTLENGTH, PRINTWORDS, PRINTWORDCOUNT, SET,
    REVERSE, ID, CONSTANT, LITERAL, PLUS, END, EOF
}
